[project]
name = "action-black"
version = "0.0.0"
requires-python = ">=3.13,<4"
dependencies = [
  "black (==25.1.0)"
]

[build-system]
requires = ["uv_build>=0.7.1,<1.0.0"]
build-backend = "uv_build"
